package com.example.spotify.modelo;

public class Reproductor {

        /*
    public static void recuperarMP3Bytes(byte[] mp3Data) {
        FileInputStream fis = new FileInputStream(file);
        return new byte[(int) file.length()];
    }
    */

}
